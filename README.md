# forum_meteor

Install
-------
    git clone ...
    meteor remove blaze-html-templates
    meteor remove ecmascript
    meteor add angular


Install Meteor
--------------
    curl https://install.meteor.com/ | sh